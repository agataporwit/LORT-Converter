//
//  ContentView.swift
//  LORT Converter
//
//  Created by Agata Porwit on 8/3/23.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        ZStack {
            //backgrounnd
            Image("background")
                .resizable()
                .edgesIgnoringSafeArea(.all)
            VStack {
                // pony image
                
                // currancy exch.
                
                // conversion
                HStack {
                    // left conversion
                    VStack {
                        //currency
                        HStack {
                            //currancy image
                            
                            //currancy text
                            
                        }
                        //text filed
                    }
                    // equal sign
                    
                    // right conv. section
                    
                    VStack {
                        
                        // currency text
                        
                        //Currancy image
                        
                    }
                    //text field
                }
                //info butotn
            }
        }
    }
    
    #Preview {
        ContentView()
    }
}
